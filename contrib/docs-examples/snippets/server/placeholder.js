function example() {
  // This is just a placeholder until other server-specific examples are added.
  // Snippets added to this directory will override the snippet found in the
  // directory above in the server SDK docs.
}
