function example() {
  // This is just a placeholder until other react-native-specific examples are added.
  // Snippets added to this directory will override the snippet found in the
  // directory above in the react-native SDK docs.
}
