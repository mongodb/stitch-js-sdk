function example() {
  // This is just a placeholder until other browser-specific examples are added.
  // Snippets added to this directory will override the snippet found in the
  // directory above in the browser SDK docs.
}
